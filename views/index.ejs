<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Supplier Product Management</title>
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('partials/header') %>

    <div class="content-wrapper">
      <h1>Product List</h1>

      <form method="GET" action="/">
        <label for="supplierId">Filter by Supplier:</label>
        <select name="supplierId" id="supplierId" onchange="this.form.submit()">
          <option value="">All Suppliers</option>
          <% suppliers.forEach(supplier => { %>
            <option value="<%= supplier._id %>" <%= selectedSupplier == supplier._id ? 'selected' : '' %>><%= supplier.name %></option>
          <% }) %>
        </select>

        <label for="search">Search Product:</label>
        <input type="text" name="search" id="search" value="<%= search %>" placeholder="Enter product name" />
        <button type="submit">Search</button>
      </form>

      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>Name</th>
            <th>Supplier</th>
            <th>Price</th>
            <th>Quantity</th>
          </tr>
        </thead>
        <tbody>
          <% if (products.length === 0) { %>
            <tr>
              <td colspan="4">No products found.</td>
            </tr>
          <% } else { %>
            <% products.forEach(product => { %>
              <tr>
                <td><%= product.name %></td>
                <td><%= product.supplier ? product.supplier.name : 'N/A' %></td>
                <td><%= product.price.toFixed(2) %></td>
                <td><%= product.quantity %></td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>  
    <%- include('partials/footer.ejs') %>
  </body>
</html>
